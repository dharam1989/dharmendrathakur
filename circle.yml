general:
  # branches:
  #   only:
  #     - master
experimental:
  notify:
    branches:
      only:
        - master
machine:
  php:
    version: 5.4.5

deployment:
  production:
    branch: master
    heroku:
	appname: wd-app-test
    